{
  "name": "tanstack-aspnet-data-monorepo",
  "private": true,
  "packageManager": "pnpm@9.9.0",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
